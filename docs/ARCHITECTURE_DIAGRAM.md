# 🏗️ Application Architecture Diagram

## 📱 Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          MOBILE APP (React Native/Expo)                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  SCREENS (app/)                                                 │   │
│  │  ├─ (tabs)/index.tsx          → Dashboard Home                 │   │
│  │  ├─ (tabs)/connect-exchange.tsx → Connect Exchanges            │   │
│  │  ├─ (tabs)/explore.tsx        → Explore Features               │   │
│  │  └─ (auth)/login.tsx          → Authentication                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              ↓ uses ↓                                   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  COMPONENTS (src/components/)                                   │   │
│  │  ├─ trading/                                                    │   │
│  │  │  ├─ AssetsCard.tsx         → Displays wallet balances       │   │
│  │  │  ├─ OpenOrdersCard.tsx     → Displays active orders         │   │
│  │  │  └─ TradingBotCard.tsx     → Bot status & control           │   │
│  │  └─ drawers/                                                    │   │
│  │     └─ ExchangeDrawer.tsx     → Exchange selector drawer       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              ↓ uses ↓                                   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  CONTEXTS (src/contexts/)                                       │   │
│  │  ├─ AuthContext              → User auth state, JWT tokens     │   │
│  │  ├─ ExchangeContext          → Selected exchange, credentials  │   │
│  │  └─ ThemeContext             → Light/Dark mode                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              ↓ calls ↓                                  │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  API SERVICES (src/services/api/)                               │   │
│  │  ├─ client.ts                → Axios HTTP client + JWT inject  │   │
│  │  ├─ auth.api.ts              → Login, register, logout         │   │
│  │  ├─ credentials.api.ts       → Save exchange API keys          │   │
│  │  ├─ binance.api.ts           → Binance exchange endpoints      │   │
│  │  └─ bitget.api.ts            → Bitget exchange endpoints       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              ↓ HTTP + JWT ↓                             │
└─────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ HTTPS Request
                                      │ Header: Authorization: Bearer <JWT>
                                      ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                       BACKEND API (NestJS)                              │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  AUTHENTICATION LAYER                                           │   │
│  │  ├─ JWTGuard                 → Validates JWT token              │   │
│  │  ├─ Extracts userId from token                                  │   │
│  │  └─ Attaches user to request object                             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              ↓ passes to ↓                              │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  CONTROLLERS (src/)                                             │   │
│  │  ├─ auth/auth.controller.ts                                     │   │
│  │  │   → POST /auth/login, /auth/register                         │   │
│  │  ├─ apicredentials/apicredentials.controller.ts                 │   │
│  │  │   → POST /credentials, GET /credentials                      │   │
│  │  ├─ binance/binance.controller.ts                               │   │
│  │  │   → GET /binance/account-balances                            │   │
│  │  │   → GET /binance/open-orders                                 │   │
│  │  │   → GET /binance/account-info                                │   │
│  │  └─ bitget/controllers/                                         │   │
│  │      ├─ account.controller.ts                                   │   │
│  │      │   → GET /bitget/account/spot/assets                      │   │
│  │      └─ orders.controller.ts                                    │   │
│  │          → GET /bitget/order/all-open-orders                    │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              ↓ calls ↓                                  │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  SERVICES (src/)                                                │   │
│  │  ├─ binance.service.ts       → Binance API logic               │   │
│  │  ├─ bitget/account.service.ts → Bitget account logic            │   │
│  │  └─ apicredentials.service.ts                                   │   │
│  │      ├─ getUserCredential(userId, 'binance')                    │   │
│  │      └─ Returns encrypted API keys from DB                      │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              ↓ queries ↓                                │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  DATABASE (PostgreSQL)                                          │   │
│  │  ├─ users               → User accounts, JWT refresh tokens    │   │
│  │  └─ api_credentials     → Exchange API keys (encrypted)         │   │
│  │      ├─ userId                                                  │   │
│  │      ├─ exchange         (binance, bitget, etc.)                │   │
│  │      ├─ apiKey           (encrypted)                            │   │
│  │      ├─ secretKey        (encrypted)                            │   │
│  │      └─ passphrase       (encrypted, for Bitget)                │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              ↓ fetches keys ↓                           │
└─────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ HTTPS Request with API Keys
                                      ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                        EXCHANGE APIs                                    │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌────────────────────────┐         ┌────────────────────────┐         │
│  │  BINANCE API           │         │  BITGET API            │         │
│  │  api.binance.com       │         │  api.bitget.com        │         │
│  ├────────────────────────┤         ├────────────────────────┤         │
│  │ GET /api/v3/account    │         │ GET /api/v2/spot/      │         │
│  │ → Account balances     │         │     account/assets     │         │
│  │                        │         │ → Spot assets          │         │
│  │ GET /api/v3/openOrders │         │                        │         │
│  │ → Active orders        │         │ GET /api/v2/spot/      │         │
│  │                        │         │     trade/unfilled-    │         │
│  │ GET /api/v3/myTrades   │         │     orders             │         │
│  │ → Trade history        │         │ → Open orders          │         │
│  └────────────────────────┘         └────────────────────────┘         │
│                                                                         │
│  Returns JSON data:                                                    │
│  ├─ Wallet balances (available, locked)                                │
│  ├─ Open orders (symbol, price, quantity, status)                      │
│  └─ Trade history (executed trades, PnL)                                │
└─────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ JSON Response
                                      ↓
                              ┌─────────────┐
                              │   Mobile    │
                              │     UI      │
                              │  Updates!   │
                              └─────────────┘
```

---

## 🔐 Authentication Flow

```
┌──────────────┐
│ User Opens   │
│ Mobile App   │
└──────┬───────┘
       │
       ↓
┌──────────────────────────┐
│ Check SecureStore for    │      ┌──────────────────┐
│ JWT Token                │ ──→  │ Token exists?    │
└──────────────────────────┘      └────┬─────────────┘
                                       │
                   ┌───────────────────┴───────────────┐
                   │ YES                               │ NO
                   ↓                                   ↓
         ┌─────────────────┐                 ┌─────────────────┐
         │ Auto-login      │                 │ Show Login      │
         │ Show Dashboard  │                 │ Screen          │
         └─────────────────┘                 └────┬────────────┘
                                                  │
                                                  ↓
                                         ┌─────────────────┐
                                         │ User enters     │
                                         │ email/password  │
                                         └────┬────────────┘
                                              │
                                              ↓
                                    ┌──────────────────────┐
                                    │ POST /auth/login     │
                                    │ with credentials     │
                                    └────┬─────────────────┘
                                         │
                                         ↓
                              ┌──────────────────────────┐
                              │ Backend validates        │
                              │ Returns JWT tokens       │
                              │ ├─ accessToken (7 days)  │
                              │ └─ refreshToken (14 days)│
                              └────┬─────────────────────┘
                                   │
                                   ↓
                         ┌──────────────────────┐
                         │ Store tokens in      │
                         │ SecureStore          │
                         └────┬─────────────────┘
                              │
                              ↓
                    ┌──────────────────────┐
                    │ Navigate to Dashboard │
                    └───────────────────────┘
```

---

## 📊 Data Fetching Flow (Example: Get Balances)

```
┌────────────────────┐
│  User selects      │
│  Binance exchange  │
│  from drawer       │
└─────────┬──────────┘
          │
          ↓
┌────────────────────────────┐
│ ExchangeContext updates:   │
│ selectedExchange='binance' │
└─────────┬──────────────────┘
          │
          ↓
┌────────────────────────────┐
│ AssetsCard component       │
│ detects change in          │
│ selectedExchange           │
└─────────┬──────────────────┘
          │
          ↓
┌────────────────────────────┐
│ useEffect triggers         │
│ fetchAssets()              │
└─────────┬──────────────────┘
          │
          ↓
┌────────────────────────────┐
│ Call:                      │
│ binanceApi.                │
│   getAccountBalances()     │
└─────────┬──────────────────┘
          │
          ↓
┌────────────────────────────┐
│ Axios HTTP GET:            │
│ /binance/account-balances  │
│ Header:                    │
│ Authorization:             │
│   Bearer eyJhbGc...        │
└─────────┬──────────────────┘
          │
          ↓
┌────────────────────────────┐
│ Backend JWTGuard:          │
│ ├─ Validates token         │
│ ├─ Extracts userId         │
│ └─ Attaches to req.user    │
└─────────┬──────────────────┘
          │
          ↓
┌────────────────────────────┐
│ BinanceController:         │
│ getAccountBalances()       │
│ ├─ Gets userId from req    │
│ └─ Calls service           │
└─────────┬──────────────────┘
          │
          ↓
┌────────────────────────────┐
│ ApiCredentialsService:     │
│ getUserCredential(         │
│   userId, 'binance'        │
│ )                          │
│ ├─ Queries database        │
│ ├─ Decrypts API keys       │
│ └─ Returns apiKey,         │
│     secretKey              │
└─────────┬──────────────────┘
          │
          ↓
┌────────────────────────────┐
│ BinanceService:            │
│ getBalances(apiKey,        │
│             secretKey)     │
│ ├─ Signs request           │
│ └─ Calls Binance API       │
└─────────┬──────────────────┘
          │
          ↓
┌────────────────────────────┐
│ BINANCE API:               │
│ GET /api/v3/account        │
│ ├─ Validates signature     │
│ └─ Returns JSON:           │
│     [                      │
│       {                    │
│         asset: "BTC",      │
│         free: "0.15",      │
│         locked: "0.0"      │
│       },                   │
│       ...                  │
│     ]                      │
└─────────┬──────────────────┘
          │
          ↓
┌────────────────────────────┐
│ Response flows back        │
│ through all layers         │
└─────────┬──────────────────┘
          │
          ↓
┌────────────────────────────┐
│ AssetsCard receives data:  │
│ ├─ Filters zero balances   │
│ ├─ Maps to unified format  │
│ ├─ Updates state           │
│ └─ Triggers re-render      │
└─────────┬──────────────────┘
          │
          ↓
┌────────────────────────────┐
│ UI UPDATES!                │
│ User sees real balances:   │
│ ┌────────────────────────┐ │
│ │ 💰 Assets              │ │
│ ├────────────────────────┤ │
│ │ ⚪ BTC    0.15         │ │
│ │ ⚪ USDT   1234.56      │ │
│ │ ⚪ ETH    2.50         │ │
│ └────────────────────────┘ │
└────────────────────────────┘
```

---

## 🔄 Error Handling Flow

```
┌────────────────┐
│ API Call Made  │
└────┬───────────┘
     │
     ↓
┌────────────────────┐      ┌──────────────┐
│ Response received? │ ──→  │ SUCCESS      │
└────┬───────────────┘      │ ├─ Parse data│
     │                      │ ├─ Update UI │
     │ ERROR                │ └─ Show data │
     ↓                      └──────────────┘
┌────────────────────┐
│ Check error type   │
└────┬───────────────┘
     │
     ├─ 401 Unauthorized
     │  ↓
     │  ┌──────────────────┐
     │  │ Try refresh token│
     │  │ If fail → Logout │
     │  └──────────────────┘
     │
     ├─ 404 Not Found
     │  ↓
     │  ┌──────────────────┐
     │  │ Show "No data"   │
     │  │ Empty state      │
     │  └──────────────────┘
     │
     ├─ 500 Server Error
     │  ↓
     │  ┌──────────────────┐
     │  │ Show error msg   │
     │  │ + Retry button   │
     │  └──────────────────┘
     │
     └─ Network Error
        ↓
        ┌──────────────────┐
        │ Show "Network    │
        │ error" + Retry   │
        └──────────────────┘
```

---

## 🎨 Component Hierarchy

```
Dashboard Screen (index.tsx)
│
├─ Header
│  ├─ Hamburger Menu Button
│  ├─ Exchange Indicator (Binance/Bitget)
│  └─ Notification Bell
│
├─ Welcome Section
│  └─ "Welcome back, {username}"
│
├─ No Exchange Warning (if no exchange selected)
│  └─ "Connect your exchange to start trading"
│
├─ TradingBotCard (if exchange connected)
│  ├─ Bot Status (Active/Idle)
│  ├─ Start/Stop Button
│  └─ Live Profit Animation
│
├─ AssetsCard (if exchange selected) ← NEW!
│  ├─ Total Value Summary
│  ├─ Asset List
│  │  └─ For each asset:
│  │     ├─ Coin Icon
│  │     ├─ Coin Name
│  │     ├─ Available Balance
│  │     ├─ Locked Balance
│  │     └─ Total Balance
│  ├─ Expand/Collapse Button
│  └─ Refresh Button
│
├─ OpenOrdersCard (if exchange selected) ← NEW!
│  ├─ Order Count Badge
│  ├─ Orders List
│  │  └─ For each order:
│  │     ├─ Symbol (BTCUSDT)
│  │     ├─ Side Badge (BUY/SELL)
│  │     ├─ Status Badge (NEW/PARTIAL)
│  │     ├─ Order Details
│  │     │  ├─ Type
│  │     │  ├─ Price
│  │     │  ├─ Quantity
│  │     │  └─ Filled %
│  │     └─ Cancel Button
│  ├─ Expand/Collapse Button
│  └─ Refresh Button
│
├─ Quick Actions
│  ├─ Manage Exchanges
│  └─ Start Bot
│
├─ Connected Exchanges Preview
│  └─ Horizontal Scroll of Exchange Icons
│
└─ ExchangeDrawer (Hamburger Menu)
   ├─ Exchange List
   ├─ Select Exchange
   └─ Add New Exchange Button
```

---

## 🎯 Summary

This architecture provides:
- ✅ **Separation of concerns** (UI → Context → API → Backend → Exchange)
- ✅ **Security** (JWT tokens, encrypted API keys)
- ✅ **Scalability** (Easy to add more exchanges)
- ✅ **Type safety** (TypeScript throughout)
- ✅ **Error resilience** (Multiple layers of error handling)
- ✅ **User experience** (Loading states, animations, refresh)

All data flows from **Exchange APIs → Backend → Frontend → UI** with proper authentication at every step! 🚀
